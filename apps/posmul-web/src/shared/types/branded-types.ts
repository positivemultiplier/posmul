export * from "../legacy-compatibility";

// Re-export commonly used types from SDK
export type {
  UserId,
  PredictionGameId,
  PredictionId,
  PmpAmount,
  PmcAmount,
  TransactionId,
} from "@posmul/auth-economy-sdk";
